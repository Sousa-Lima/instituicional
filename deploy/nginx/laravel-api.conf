# Nginx → PHP-FPM (serviço Swarm `slc_app:9000`). Imagem PHP só corre FPM, sem HTTP na :80.
server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /var/www/html/public;
    index index.php;
    charset utf-8;
    client_max_body_size 100M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }
    location = /robots.txt {
        access_log off;
        log_not_found off;
    }

    # Resolve o upstream via DNS interno do Docker Swarm a cada 10s para evitar IP
    # cacheado após update/restart do serviço slc_app.
    resolver 127.0.0.11 valid=10s ipv6=off;

    location ~ \.php$ {
        set $upstream slc_app;
        fastcgi_pass $upstream:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
        fastcgi_param HTTP_PROXY "";
        # TLS termina no Traefik; Laravel usa TRUSTED_PROXIES + X-Forwarded-*
        fastcgi_param HTTPS on;
        fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        fastcgi_param HTTP_X_FORWARDED_FOR $proxy_add_x_forwarded_for;
    }
}
